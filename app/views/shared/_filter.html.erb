<div data-controller="filter" class="w-full">
  
  <!-- Estado 2: Múltiplos filtros e botão de filtros ativos -->
  <div data-filter-target="state2" class="border-2 border-dashed border-yellow-400 rounded-2xl bg-yellow-50 p-6">
    <div class="flex items-center gap-4 flex-wrap">
      <span class="text-orange-600 text-lg">🏷️</span>
      <span class="text-gray-700 italic font-medium">Filtrar por:</span>
      
      <div class="flex gap-3">
        <button 
          class="filter-btn flex items-center gap-2 px-6 py-2 bg-pink-100 text-pink-600 rounded-full border-2 border-yellow-400 hover:bg-pink-200 transition-all font-medium shadow-sm">
          <span>❤️</span>
          <span>Todos</span>
        </button>

        <button 
          data-action="click->filter#toggleFilter"
          data-filter-name="familia"
          class="filter-btn flex items-center gap-2 px-6 py-2 bg-pink-100 text-pink-600 rounded-full border-2 border-yellow-400 hover:bg-pink-200 transition-all font-medium shadow-sm">
          <span>❤️</span>
          <span>Família</span>
        </button>

        <button 
          data-action="click->filter#toggleFilter"
          data-filter-name="familia"
          class="filter-btn flex items-center gap-2 px-6 py-2 bg-pink-100 text-pink-600 rounded-full border-2 border-yellow-400 hover:bg-pink-200 transition-all font-medium shadow-sm">
          <span>❤️</span>
          <span>Família</span>
        </button>
        
      </div>
    </div>

    <!-- Botão de filtros ativos -->
    <div class="mt-4">
      <button 
        class="hidden flex items-center gap-2 px-5 py-2 bg-gradient-to-r from-pink-500 to-pink-400 text-white rounded-full hover:from-pink-600 hover:to-pink-500 transition-all font-medium shadow-md">
        <span>→</span>
        <span>2 filtros ativos</span>
        <span>❤️</span>
      </button>
    </div>
  </div>
</div>
